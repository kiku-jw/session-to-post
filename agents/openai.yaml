interface:
  display_name: Session to Post
  short_description: Turn real session artifacts into a durable wrap-up or post
  default_prompt: Use $session-to-post to turn this completed coding session into a grounded wrap-up or post draft based on the real artifacts.
